name: Preflight

on:
  workflow_call:
    secrets:
      token:
        description: "The Broker token"
        required: true
    inputs:
      release_check:
        description: "Whether to run the release package check"
        required: false
        default: false
        type: boolean
permissions: {}

jobs:
  check-jwt:
    name: Check broker token
    uses: ./.github/workflows/check-token.yaml
    secrets:
      token: ${{ secrets.token }}

  check-release-package:
    name: Check release package
    uses: ./.github/workflows/check-release-package.yaml
    secrets:
      token: ${{ secrets.token }}
    if: ${{ github.ref_type == 'tag' && inputs.release_check == true }}
